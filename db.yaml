mysql_host: 'localhost'
# change as per the given username for mysql
mysql_user: 'root'
# replace the **** with the the password for the mysql
mysql_password: '*****'
# database not yet configured
#  if you created a database with name flask_app the configuration should be as follows
mysql_db: 'flask_app'
